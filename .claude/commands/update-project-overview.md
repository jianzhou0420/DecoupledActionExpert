Update the project overview documentation by re-scanning the repository.

## Steps:
1. Scan the repository structure (directories, key files, new additions)
2. Identify any changes to entry points, configs, policies, or scripts
3. Update `.claude/PROJECT_OVERVIEW.md` with current state
4. Run `/collect-commands` to refresh `.claude/cmds.sh`
5. Summarize what changed
